import React from 'react';

const Error = () => (
    <>
        <h1 className="display-4 text-danger">Error!</h1>
        <p className="lead">TFN validation tool doesn’t allow multiple attempts for similar TFNs</p>
    </>
);

export default Error;
